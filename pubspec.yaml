name: mahfathati
version: 1.0.0+1
description: تطبيق إدارة الحسابات الشخصية والمالية مع نظام قيود مزدوجة.
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  provider: ^6.1.2
  fl_chart: ^0.68.0
  sqflite: ^2.3.3+1
  path: ^1.9.0
  local_auth: ^2.2.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
